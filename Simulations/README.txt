These folders hold the files that are created at the end of each exercise.

Each exercise starts with a blank folder (with files generated via the server.py
tool) or with copying the files from the previous step
